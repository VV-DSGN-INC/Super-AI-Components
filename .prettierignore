pnpm-lock.yaml
apps/docs/public/r/
apps/docs/.next/
apps/storybook/storybook-static/
node_modules/
.turbo/
playwright-report/
test-results/

# Harvested-verbatim files: pnpm format would rewrite their upstream style
# and silently break the byte-provenance discipline (see packages/ds-rules/README.md).
packages/ds-rules/

# Same reasoning, and the same discipline: tools/ds-architecture is a verbatim
# copy of the upstream checker (stamp in its VENDOR.md), written in upstream's
# own style. Formatting it here would make every future refresh a whole-tree
# diff and hide the real change. VENDOR.md records the one local edit.
tools/ds-architecture/

# Generated by apps/docs/scripts/gen-wiring.mts and byte-compared by
# check:contract. Prettier reformats them, the byte comparison then fails, and
# running gen:wiring to fix that re-emits files prettier wants to reformat
# again. The generator owns their shape. This was the ONLY thing standing
# between this repo and a prettier-clean tree — not the guidance regexes the
# bash hook used to blame, which survive formatting untouched.
apps/docs/lib/demos.generated.ts
apps/docs/lib/docs.generated.ts

# Derived by apps/docs/scripts/lib/contract-emit.test.ts (`pnpm contract:emit`)
# and byte-compared by the same test. The emitter owns their shape.
apps/docs/registry/super-ai/*.meta.json
apps/docs/index/components.toon
apps/docs/public/llms.txt
apps/docs/public/llms-full.txt
apps/docs/public/llms/
